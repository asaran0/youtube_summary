# ── Core media/image processing ─────────────────────────────
Pillow>=10.0.0
numpy>=1.24.0
moviepy>=2.0.0

# ── Deep learning backend (used by mms and xtts TTS strategies) ──
# On Apple Silicon: torch automatically uses MPS (Metal GPU)
torch>=2.1.0

# ── Hindi neural TTS — "mms" backend ──
# Provides VitsModel / AutoTokenizer (core/tts/mms_strategy.py)
transformers>=4.35.0

# ── Audio array -> WAV file writing (used by "mms" and "xtts") ──
scipy>=1.11.0

# ── Voice cloning TTS — "xtts" backend ──
# Optional: only needed if a mode's config sets TTS_BACKEND = "xtts".
# Downloads a ~2 GB model on first run. Uncomment to install:
# TTS>=0.22.0
# soundfile is the torchaudio.load fallback when torchaudio 2.6+ uses torchcodec.
# Install alongside TTS:  pip install soundfile
soundfile>=0.12.0

# Note: "macos" backend (the default in both story_mode/config.py and
# qa_mode/config.py) needs no extra Python packages — it uses the
# built-in macOS `say` command.

# ── API backend ──────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
